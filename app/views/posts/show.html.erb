<h1>Here's a post by <%= @post.user.email %></h1>

<h2><%= @post.title %></h2>

<p><%= @post.body  %></p>

<% if @post.user.id === session[:user_id] %>
  <%= link_to  "Edit this post", edit_post_path(@post.id) %>
<% end %>
<%= link_to  "Back to posts", posts_path %>
